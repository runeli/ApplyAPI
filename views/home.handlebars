<!DOCTYPE html>
<html lang="en">
<head>
    <meta http-equiv="content-type" content="text/html; charset=UTF-8">
    <meta charset="utf-8">
    <title>{{config.pageTitle}}</title>
    <meta name="description" content="{{config.pageDesc}}" />
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <!--[if lt IE 9]>
        <script src="//html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
    <link href="/css/styles.css" rel="stylesheet">
</head>
<body>
    <div class="wrapper">
        <div class="box" style="background-image:url({{config.backgroundURL}})">
            <div class="row">

                <!-- sidebar -->
                <div class="column col-sm-3" id="sidebar">
                    <h3>{{config.pageTitle}}</h3>
                    <p class="col-sm-8">{{{config.pageDesc}}}</p>
                </div>
                <!-- /sidebar -->
                <!-- main -->
                <div class="column col-sm-9" id="main">
                    <div class="padding">
                        <div class="full col-sm-9">

                            <!-- content -->

                            <div class="col-sm-12" id="featured">
                                <div class="page-header text-muted">
                                    How to apply?
                                </div>
                            </div>

                            <!--/top story-->
                            <div class="row">
                                <div class="col-sm-10">
                                    <h3>Send a HTTP-request containing your application information</h3>
                                    <p>Send a <strong class="text-uppercase">HTTP {{config.method}}</strong> request with Content-type type of <code>{{config.mime}}</code> to <code>{{config.base}}/</code> </p><p>If everything is good to go you will receive a HTTP 200 response and a message indicating that your application has been saved.</p>
                                    <p>Make sure you format your JSON object exactly as spesified in the table below.</p>
                                    <table class="table">
                                        <thead>
                                            <tr>
                                                <th>Field name</th><th>Datatype</th><th>Mandatory</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            {{#each fields}}
                                            <tr>
                                                <td><span class="desc" title="{{this.desc}}">{{this.label}}</span><td><code>{{this.typeString}}</code></td><td>{{#if this.required}}Yes{{else}}No{{/if}}</td>
                                            </tr>
                                            {{/each}}
                                        </tbody>
                                    </table>
                                    

                                </div>
                            </div>
                            
                            <div class="col-sm-12">
                                {{#if config.showApplicantCount}}
                                <div class="page-header text-muted divider">
                                    {{appCount}} applicants have already applied
                                </div>
                                {{/if}}
                                <p>For more information, see our website at <strong><a href="{{config.organizationURL}}">{{config.organizationURL}}</a></strong></p>
                            </div>
                            <hr />
                            


                        </div><!-- /col-9 -->
                    </div><!-- /padding -->
                </div>
                <!-- /main -->

            </div>
        </div>
    </div>
    <!-- script references -->
    <script src="//ajax.googleapis.com/ajax/libs/jquery/2.0.2/jquery.min.js"></script>
    <script src="/js/bootstrap.min.js"></script>
    <script>
        $(function () {
            $('td .desc').tooltip();
        })
    </script>

</body>
</html>